<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Client.Views.Phone.PhoneEditGamePage"
             Title="Edit Game"
             BackgroundColor="White"
             Padding="15"
             xmlns:inputLayout="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs">

    <VerticalStackLayout Spacing="12">

        <Label Text="Edit Game" FontSize="24" HorizontalOptions="Center" FontAttributes="Bold" />

        <inputLayout:SfTextInputLayout Hint="Game Name">
            <Entry x:Name="nameEntry" />
        </inputLayout:SfTextInputLayout>

        <inputLayout:SfTextInputLayout Hint="Game Description">
            <Editor x:Name="descriptionEditor" AutoSize="TextChanges" />
        </inputLayout:SfTextInputLayout>

        <inputLayout:SfTextInputLayout Hint="Genre">
            <editors:SfComboBox x:Name="genreComboBox" DisplayMemberPath="Name" TextMemberPath="Name" />
        </inputLayout:SfTextInputLayout>

        <inputLayout:SfTextInputLayout Hint="Price">
            <Entry x:Name="priceEntry" />
        </inputLayout:SfTextInputLayout>

        <inputLayout:SfTextInputLayout Hint="Release Date">
            <DatePicker x:Name="datePickerControl" />
        </inputLayout:SfTextInputLayout>

        <VerticalStackLayout Spacing="8">
            <Label Text="Current Image:" FontSize="12" FontAttributes="Bold" />
            <Image x:Name="imagePreview" HeightRequest="150" WidthRequest="150" HorizontalOptions="Center" Aspect="AspectFill" />
        </VerticalStackLayout>

        <HorizontalStackLayout Spacing="10" Margin="0,20,0,0" HorizontalOptions="Center">
            <Button Text="Save" Clicked="OnSaveClicked" BackgroundColor="Green" TextColor="White" WidthRequest="100" />
            <Button Text="Cancel" Clicked="OnCancelClicked" BackgroundColor="Gray" TextColor="White" WidthRequest="100" />
        </HorizontalStackLayout>

    </VerticalStackLayout>

</ContentPage>
